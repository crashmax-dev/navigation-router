import{V as e,et as t,mt as n,tt as r}from"./chunks/framework.DaWB_xiL.js";var i=JSON.parse(`{"title":"Introduction","description":"","frontmatter":{},"headers":[],"relativePath":"guide/introduction.md","filePath":"guide/introduction.md"}`),a={name:`guide/introduction.md`};function o(e,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[r(`<h1 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to “Introduction”">​</a></h1><p><code>navigation-router</code> is a small client-side router for vanilla TypeScript / DOM apps.</p><p>It leans on two web platform APIs:</p><ul><li><a href="https://developer.mozilla.org/en-US/docs/Web/API/Navigation_API" target="_blank" rel="noreferrer">Navigation API</a> — intercept in-app navigations when available</li><li><a href="https://developer.mozilla.org/en-US/docs/Web/API/URL_Pattern_API" target="_blank" rel="noreferrer">URLPattern</a> — match pathnames and extract named groups</li></ul><h2 id="what-you-get" tabindex="-1">What you get <a class="header-anchor" href="#what-you-get" aria-label="Permalink to “What you get”">​</a></h2><ul><li>Class-based routes (<code>RouteComponent</code>)</li><li>Typed <code>params</code> / <code>query</code> via <code>RouteCtx</code></li><li>History backends through <code>RouterAdapter</code></li><li>Optional built-in navigation menu, or roll your own with <code>createLink</code> + <code>getRoutes</code></li><li><code>destroy()</code> for clean teardown (tests / HMR)</li></ul><h2 id="browser-support" tabindex="-1">Browser support <a class="header-anchor" href="#browser-support" aria-label="Permalink to “Browser support”">​</a></h2><table tabindex="0"><thead><tr><th>Feature</th><th>Notes</th></tr></thead><tbody><tr><td><code>URLPattern</code></td><td>Required. Available in Chromium / Safari / recent Firefox; polyfill in tests via <code>urlpattern-polyfill</code>.</td></tr><tr><td>Navigation API</td><td>Used by <code>NavigationAdapter</code> when present; falls back to <code>history</code> + <code>popstate</code>.</td></tr><tr><td>Hash routing</td><td>Use <code>HashAdapter</code> on hosts without SPA path rewrites (e.g. GitHub Pages).</td></tr></tbody></table><h2 id="mental-model" tabindex="-1">Mental model <a class="header-anchor" href="#mental-model" aria-label="Permalink to “Mental model”">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>adapter.listen(path)</span></span>
<span class="line"><span>  → match URLPattern</span></span>
<span class="line"><span>  → unmount previous</span></span>
<span class="line"><span>  → await setup(ctx)</span></span>
<span class="line"><span>  → render(ctx) → renderRoot</span></span>
<span class="line"><span>  → refresh() re-runs render when local state changes</span></span></code></pre></div><p>Adapters own the address bar (or memory). The router owns matching and the route lifecycle.</p>`,11)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};